{
  "name": "markd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "check": "tsc --noEmit && node esbuild.mjs --check",
    "test": "vitest run",
    "test:e2e": "vitest run tests/e2e"
  },
  "devDependencies": {
    "@types/chrome": "0.2.9",
    "esbuild": "0.28.2",
    "puppeteer": "25.10.0",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
